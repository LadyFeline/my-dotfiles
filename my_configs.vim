# Show number at the beginning of each line
set number

colorscheme dracula

# Search in vim
let g:ackprg = "/opt/homebrew/bin/ack -s -H --nocolor --nogroup --column --ignore-file=is:tags"

# Set tags path -- change this in the future depending on your project
set tags=tags;
